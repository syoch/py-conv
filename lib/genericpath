#pragma once
#ifndef _OS_PATH_
#define _OS_PATH_

#include "base"
//#include <vector>

class path
{
public:
	bool exists(Any path);
	bool isdir(Any path);
	bool isfile(Any path);
	//std::vector<std::string> split(Any path);
};

#endif // !_OS_PATH_