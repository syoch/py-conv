#pragma once

#include "base"
#include <initializer_list>

namespace Core
{
    dict make_dict(std::initializer_list<Any> keys,std::initializer_list<Any> values){
        dict ret;
        
        return ret;
    }
} // namespace core
